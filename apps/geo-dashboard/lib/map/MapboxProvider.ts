import type { MapProvider } from './MapProvider.js';
export class MapboxProvider implements Partial<MapProvider> {
  constructor() {
    throw new Error('MapboxProvider ainda não implementado (stub). Use o MapLibreProvider.');
  }
}
